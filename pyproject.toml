[project]
name = "yolo-complexity-lab"
version = "0.1.0"
description = "Streamlit lab for runtime and computational complexity comparison of YOLO and CNN object detectors."
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["src"]
